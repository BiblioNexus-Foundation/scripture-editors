const { verseWordsActions } = require("./verseWords");
const { stripMarkupActions } = require("./stripAlignment");
const { mergeAlignmentActions } = require("./mergeAlignement");

module.exports = {
  verseWordsActions,
  stripMarkupActions,
  mergeAlignmentActions,
};
