/**
 * View configuration helpers for hosts that do not need to mount an editor.
 * @packageDocumentation
 */
export {
  BLOCK_VERSE_VIEW_MODE,
  getDefaultViewMode,
  getDefaultViewOptions,
  getViewMode,
  getViewOptions,
  isBlockVerseLayout,
  PARAGRAPH_STRUCTURE_VIEW_MODE,
  STANDARD_VIEW_MODE,
  viewModeToViewNames,
} from "shared-react";
export type { MarkerMode, NoteMode, VerseLayout, ViewMode, ViewOptions } from "shared-react";
