// CSS-only build entry: emits dist/styles.css. The companion dist/styles.js is empty by design,
// is excluded from the published tarball and is not part of the public API — consumers import
// the stylesheet, never this module.
import "./styles.css";
