{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chrome: Launch vite for debugging",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
