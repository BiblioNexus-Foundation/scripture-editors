{
  "$schema": "https://json-schema.org/schema",
  "$id": "MarkersData",
  "title": "",
  "type": "object",
  "properties": {
    "usfmStyleUrl": {
      "type": "string",
      "description": "URL to the USFM style file",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the URL of the USFM style file?"
    },
    "outputPath": {
      "type": "string",
      "description": "Where to save the resulting file",
      "default": "packages/shared/utils/"
    }
  },
  "required": ["usfmStyleUrl"]
}
